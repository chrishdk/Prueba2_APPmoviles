import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'Prueba2_APPmoviles',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
